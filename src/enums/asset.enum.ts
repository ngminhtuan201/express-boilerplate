export enum AssetType {
  TTS = "tts",
  MUSIC = "music",
  SFX = "sfx",
  VIDEO = "video",
  IMAGE = "image",
}

export enum AssetGenerationStatus {
  PENDING = "pending",
  PROCESSING = "processing",
  COMPLETED = "completed",
  FAILED = "failed",
}
